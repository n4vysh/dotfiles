session_name: music
suppress_history: true
start_directory: $HOME
windows:
  - panes:
      - ncspot
  # HACK: avoid "No previous window" error
  # https://github.com/tmux-python/tmuxp/issues/364
  - panes:
      - exit
