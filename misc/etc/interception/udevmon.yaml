---
# yamllint disable-line rule:line-length
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/dual-function-keys/xcape.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "AT Translated Set 2 keyboard"
    EVENTS:
      EV_KEY: [KEY_RIGHTALT, KEY_LEFTALT]
