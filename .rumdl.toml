[global]
disable = [
  "MD034", # GitHub Flavored Markdown (GFM) allows bare URLs
]

[MD007]
indent = 4 # for parser compatibility

[MD024]
# allow duplication with different parent headings like changelogs
siblings-only = true
